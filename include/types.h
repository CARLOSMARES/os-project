#ifndef _TYPES_H
#define _TYPES_H

#define NULL 0

// Tipos de tamaño
typedef unsigned int size_t;
typedef int ssize_t;

// Tipo para tiempo
typedef unsigned int time_t;

// Macros útiles
#define TRUE 1
#define FALSE 0

#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a, b) ((a) > (b) ? (a) : (b))

#endif